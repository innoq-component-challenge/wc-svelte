import component from "svelte-tag"

import Button from "./components/button.svelte";
new component({ component: Button, tagname: "svelte-button" })

import Badge from "./components/badge.svelte";
new component({ component: Badge, tagname: "svelte-badge" })

import Card from "./components/card.svelte";
new component({ component: Card, tagname: "svelte-card" })

import FormGroup from "./components/form-group.svelte";
//new component({ component: FormGroup, tagname: "svelte-form-group" })

import List from "./components/list.svelte";
new component({ component: List, tagname: "svelte-list" })

import MagicHeaders from "./components/magic_headers.svelte";
//new component({ component: MagicHeaders, tagname: "svelte-magic-headers" })